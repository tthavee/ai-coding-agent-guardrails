# Guardrails Validation Test

This file exists solely to test that branch protection rules and CI checks
are working correctly. It can be deleted after validation is complete.

## What this PR should verify

- [ ] CI workflow triggers automatically on PR open
- [ ] PR template loads with AI disclosure section
- [ ] Status checks appear on the PR before merge is allowed
- [ ] Merge is blocked until checks pass (or shows bypass warning for admin)
